# max depth of binary tree

I actually had to do this for a few of the previous problems implicitly or as a helper function, but it's very simple. Calculate the depth at each subtree, if the root is null it's 0, else it's 1+max(left subtree, right subtree).
