# roman to integer

Really clean solution here. The question already gives a dictionary-like set of values for a particular string. There are also some special cases where if a particular character comes before some particular characters, then the value should be different. I created a formula that essentially double subtracts just in case I overcompensated for it previously. It's a clean solution, but pretty straightforward.
