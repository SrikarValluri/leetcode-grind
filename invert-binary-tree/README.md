# invert binary tree

I've seen this type of recursion before so just tinkered and figured it out

1) check if root exists
2) if it does, call on left and right, treat recursion like black box (it'll swap the nodes below left and right)
3) swap left and right itself

pretty cool
