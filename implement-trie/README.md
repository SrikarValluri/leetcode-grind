# implement Trie


Super simple- similar to Min Stack question. I could've used a dict instead of a set as well, but I think set was better in this case. I don't think super necessary to elaborate here.
