# valid palindrome

1) remove alphanum characters, did list comprehension to turn into list
2) had a pointer on the left and on the right, iterated them to the middle and checked whether it was always matching
    (this way i didn't need to have two seperate cases for odd/even length)
