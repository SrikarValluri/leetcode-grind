# lowest common ancestor

 had to peek at to soln for this one, but makes perfect sense

 IMPORTANT: binary trees have the property such that root.left < root < root.right, and everything to the left of root is less than root.
 I didn't know that originally.

 that makes this problem straightforward- we can ignore the part of the tree that we know is not part of the problem.
 i peeked at a loop solution, did it recursively, and then did it loop

 
